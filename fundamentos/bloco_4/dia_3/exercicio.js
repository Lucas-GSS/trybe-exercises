let lines = 5;
let symbol = '*';
let inputLine = '';

for (let lineIndex = 0; lineIndex < lines; lineIndex += 1) {
  inputLine = inputLine + symbol;
};
for (let lineIndex = 0; lineIndex < lines; lineIndex += 1) {
  console.log(inputLine);
};
//solução da explicação no gabarito